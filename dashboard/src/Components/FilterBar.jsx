import { useState } from "react";

function FilterBar(currenteStatusFilter, currentCategoryFilter, onStatusFilterChange, onCategoryFilterChange){

    return(
        <>
        
        </>
    )
}

export default FilterBar;